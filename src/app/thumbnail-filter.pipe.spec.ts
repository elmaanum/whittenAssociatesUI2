import { ThumbnailFilterPipe } from './thumbnail-filter.pipe';

describe('ThumbnailFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ThumbnailFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
